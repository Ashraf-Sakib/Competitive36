#include <bits/stdc++.h>
using namespace std;
 
int main()
{
 
ios_base::sync_with_stdio(false);
cin.tie(NULL);
    // Write your code here
     int t;
     cin>>t;
     while(t--){
        int a,b,c,d;
        cin>>a>>b>>c>>d;
        bool allEqual = ((a^b) | (a^c) | (a^d)) == 0;
        if(allEqual){
            cout<<"YES"<<endl;
        }else{
            cout<<"NO"<<endl;
        }
     }
    return 0;
}